{
  "name": "root",
  "private": false,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "lerna": "lerna",
    "build": "lerna run build",
    "pack": "lerna run pack",
    "prettier": "lerna run prettier",
    "test": "lerna run test",
    "demo": "(cd packages\\perfrunner-cli\\)&&(npm run demo)"
  }
}
